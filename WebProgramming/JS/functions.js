let subFunc = function(arg1, arg2) {
    return arg1 - arg2;//Lambda expression in JS...
}//Anonymous methods in js...

let mulFunc = (arg1, arg2) => arg1 * arg2;

let divFunc = (arg1, arg2) => arg1 / arg2;