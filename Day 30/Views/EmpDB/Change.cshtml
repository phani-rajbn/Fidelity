@model SampleMvcApp.Models.Employee

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Change</title>
</head>
<body>
    <h1>Details of the Employee</h1>
    <p>HTML Helpers are C# based functions that will be executed in the server of the ASP.NET and generate an HTML tag based on the function. ActionLink returns an Achor tag, Editor would return an input type of text</p>
    <p>DisplayFor will not send its data while the Form is posted. Instead we should use HiddenFor which will send the data to the POST METHOD. HiddenFor is always invisible to the Page.</p>
    @using (Html.BeginForm())
    { 
        <div>
            <p>Employee ID: @Html.DisplayFor((m) => m.EmpID) 
            @Html.HiddenFor((m)=>m.EmpID)</p>
            <p>Employee Name: @Html.EditorFor((m) => m.EmpName)</p>
            <p>Employee Address: @Html.EditorFor((m) => m.EmpAddress)</p>
            <p>Employee Salary: @Html.EditorFor((m) => m.EmpSalary)</p>
            <p><input type="submit" value="Change" /></p>
        </div>
    }
</body>
</html>
